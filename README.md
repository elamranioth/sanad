# Sanad

Static legal-judgment browser for the Sanad app.

## Local preview

Double-click `open-sanad-local.cmd`, or run:

```powershell
node sanad-local-server.js
```

Then open:

```text
http://localhost:8787/sanad.html
```
